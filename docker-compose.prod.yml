version: "3.8"
services:
  backend:
    build: ~
    image: ${ECR_REGISTRY}/usb-control-backend:latest
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000
  frontend:
    build: ~
    image: ${ECR_REGISTRY}/usb-control-frontend:latest
    command: npm start
