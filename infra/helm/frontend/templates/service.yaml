apiVersion: v1
kind: Service
metadata:
  name: {{ include "usb-control-frontend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "usb-control-frontend.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
