apiVersion: v1
kind: Service
metadata:
  name: {{ include "usb-control-backend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "usb-control-backend.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
